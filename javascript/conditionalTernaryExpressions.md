if else

```
if (something) {
	do this
} else {
	do this instead
}
```

becomes ternary

` if something ? do this : or else this`

example of an if with assignment

``
if ( something ) this = that;
``
